(()=>{'use strict';const root=document.documentElement,preference=matchMedia('(prefers-reduced-motion: reduce)');let off=preference.matches;root.classList.toggle('motion-off',off);preference.addEventListener('change',e=>{off=e.matches;root.classList.toggle('motion-off',off);if(off)document.querySelectorAll('.motion-pending').forEach(el=>el.classList.remove('motion-pending'))});
if('IntersectionObserver'in window&&!off){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.remove('motion-pending');entry.target.classList.add('motion-in');observer.unobserve(entry.target)}}),{threshold:.08,rootMargin:'0px 0px -25px 0px'});document.querySelectorAll('main > section:not(:first-child),.section-heading,.download-card,.guide-step,.download-callout').forEach((el,i)=>{if(el.getBoundingClientRect().top<innerHeight)return;el.style.setProperty('--reveal-delay',Math.min(i%3*65,130)+'ms');el.classList.add('motion-pending');observer.observe(el)})}
const line=document.createElement('div');line.className='scroll-progress';line.setAttribute('aria-hidden','true');document.body.append(line);let waiting=false;function progress(){const max=root.scrollHeight-innerHeight;line.style.transform='scaleX('+(max>0?Math.min(1,scrollY/max):0)+')';document.querySelector('header')?.classList.toggle('is-scrolled',scrollY>30);waiting=false}addEventListener('scroll',()=>{if(!waiting){waiting=true;requestAnimationFrame(progress)}},{passive:true});addEventListener('resize',progress);progress();})();
